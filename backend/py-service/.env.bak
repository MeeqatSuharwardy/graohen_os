# FlashDash Backend Service - Environment Configuration
# Copy this file to .env and update with your actual paths
# cp env.example .env

# =============================================================================
# Server Configuration
# =============================================================================
# Host and port for the FastAPI service
PY_HOST=127.0.0.1
PY_PORT=17890

# =============================================================================
# Tool Paths (Absolute paths required)
# =============================================================================
# Path to ADB executable
# macOS/Linux: Usually /usr/local/bin/adb or /usr/bin/adb
# Windows: C:\path\to\platform-tools\adb.exe
ADB_PATH=/usr/local/bin/adb

# Path to Fastboot executable
# macOS/Linux: Usually /usr/local/bin/fastboot or /usr/bin/fastboot
# Windows: C:\path\to\platform-tools\fastboot.exe
FASTBOOT_PATH=/usr/local/bin/fastboot

# =============================================================================
# GrapheneOS Paths (Absolute paths required)
# =============================================================================
# Path to GrapheneOS source code root (synced via repo)
# Example: /home/user/grapheneos or C:\grapheneos
# Leave empty if you don't plan to build from source
GRAPHENE_SOURCE_ROOT=

# Path to directory containing GrapheneOS factory image bundles
# This is where downloaded/extracted bundles will be stored
# Example: /home/user/GrapheneOS_Bundles or C:\GrapheneOS_Bundles
# Structure: {GRAPHENE_BUNDLES_ROOT}/{codename}/{version}/
GRAPHENE_BUNDLES_ROOT=

# =============================================================================
# Logging
# =============================================================================
# Directory for log files (absolute or relative path)
# Logs will be created as: {LOG_DIR}/flasher_YYYYMMDD_HHMMSS.log
LOG_DIR=./logs

# =============================================================================
# Supported Devices
# =============================================================================
# Comma-separated list of supported Pixel device codenames
# Default includes: cheetah (Pixel 7 Pro), panther (Pixel 7), husky (Pixel 8 Pro), etc.
SUPPORTED_CODENAMES=cheetah,panther,raven,oriole,husky,shiba,akita,felix,tangorpro,lynx,bluejay,barbet,redfin

# =============================================================================
# Safety Settings
# =============================================================================
# Enable dry-run mode by default (show commands without executing)
DRY_RUN_DEFAULT=true

# Timeout for flash scripts in seconds (default: 30 minutes)
SCRIPT_TIMEOUT_SEC=1800

# Allow advanced fastboot commands (use with caution)
ALLOW_ADVANCED_FASTBOOT=false

# Require typed confirmation for dangerous operations (e.g., "UNLOCK")
REQUIRE_TYPED_CONFIRMATION=true

# =============================================================================
# Build Configuration (Optional - Linux only)
# =============================================================================
# Enable building GrapheneOS from source (requires GRAPHENE_SOURCE_ROOT)
BUILD_ENABLE=false

# Output directory for built images (absolute path)
# Leave empty to use default build output location
BUILD_OUTPUT_DIR=

# Timeout for build process in seconds (default: 4 hours)
BUILD_TIMEOUT_SEC=14400

